language: php

php:
    - 5.3
    - 5.4

before_script:
    - sudo aptitude search c-client
    - sudo apt-get install php5-imap
    - wget http://getcomposer.org/composer.phar
    - php composer.phar install --dev
